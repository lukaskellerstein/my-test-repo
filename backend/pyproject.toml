[project]
name = "todo-backend"
version = "0.1.0"
description = "Simple TODO app backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.2",
    "uvicorn>=0.42.0",
]

[tool.uv]
dev-dependencies = []
